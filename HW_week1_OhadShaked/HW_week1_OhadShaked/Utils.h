#ifndef UTILS_H
#define UTILS_H

void reverse(int* nums, unsigned int size); // Reverses the numbers stored in a given array using the stack structure.
int* reverse10(); // Takes input for 10 numbers in a array, reverses the array, and returns the reversed array.

#endif // UTILS_H